MLPs for KR Sequence Classifications
filtered_embeddings.pt -> xiang
filtered_ccad_embeddings.pt -> clustercad
ccad_model-exp(1) -> trained on clustercad dataset
xiang_model -> trained on xiang dataset

filtered_ccad_embeddings.pt and filtered_embeddings.pt (xiang) were not pushed due to size constraints.